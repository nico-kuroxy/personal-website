/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://kuroxy-website.netlify.app/', // Replace with your domain
    generateRobotsTxt: true, // Also creates robots.txt
    // Optional: Exclude private paths
    exclude: [''],
  }